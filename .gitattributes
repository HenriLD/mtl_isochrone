# Normalise line endings in the repo (avoids the LF/CRLF churn on Windows).
* text=auto

# Binary images via Git LFS (the -text overrides the rule above).
docs/images/*.jpg filter=lfs diff=lfs merge=lfs -text
web/images/quests/*.jpg filter=lfs diff=lfs merge=lfs -text
